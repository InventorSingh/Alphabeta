---
import ProductsList from "./ProductsList.astro";

const toggleChatbot = () => {
    const chatbot = document.getElementById('chatbot');
    if (chatbot) {
        chatbot.style.display = chatbot.style.display === 'none' ? 'flex' : 'none';
    }
};

const sendMessage = () => {
    const userInput = document.getElementById('user-input');
    const messages = document.getElementById('chatbot-messages');

    if (!userInput || !messages) {
        console.error('Required DOM elements are missing.');
        return;
    }

    if (userInput.value.trim() === '') return;

    const userMessage = document.createElement('div');
    userMessage.textContent = userInput.value;
    userMessage.style.textAlign = 'right';
    userMessage.style.margin = '10px';
    userMessage.style.backgroundColor = '#e0f7fa';
    userMessage.style.padding = '10px';
    userMessage.style.borderRadius = '8px';

    messages.appendChild(userMessage);

    const botMessage = document.createElement('div');
    botMessage.textContent = `You said: "${userInput.value}". I'm here to assist you further.`;
    botMessage.style.margin = '10px';
    botMessage.style.backgroundColor = '#f1f1f1';
    botMessage.style.padding = '10px';
    botMessage.style.borderRadius = '8px';

    messages.appendChild(botMessage);

    userInput.value = '';
};
---

<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Alphabeta - Building a Greener Future</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        margin: 0;
        padding: 0;
        color: #333;
        line-height: 1.6;
        background: linear-gradient(to bottom, #e8f5e9, #ffffff);
      }

      header {
        background: #2e7d32;
        color: white;
        padding: 1rem 0;
        text-align: center;
      }

      header h1 {
        font-size: 3rem;
        font-family: 'Georgia', serif;
      }

      nav {
        background: #1b5e20;
        color: white;
        display: flex;
        justify-content: center;
        padding: 0.8rem 0;
      }

      nav a {
        color: white;
        text-decoration: none;
        margin: 0 1.5rem;
        font-size: 1.2rem;
        font-weight: bold;
        transition: color 0.3s;
      }

      nav a:hover {
        color: #a5d6a7;
      }

      section {
        padding: 3rem;
        text-align: center;
        position: relative;
      }

      section h2 {
        font-size: 2.5rem;
        margin-bottom: 1.5rem;
        color: #388e3c;
      }

      section p {
        max-width: 800px;
        margin: 0 auto;
        font-size: 1.1rem;
        position: relative;
        z-index: 1;
      }

      .svg-background {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        z-index: 0;
        opacity: 0.3;
      }

      .chatbot-container {
        position: fixed;
        bottom: 20px;
        right: 20px;
        width: 300px;
        max-height: 400px;
        display: flex;
        flex-direction: column;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
        background-color: #fff;
        border-radius: 8px;
        overflow: hidden;
      }

      .chatbot-header {
        background: #2e7d32;
        color: white;
        padding: 10px;
        text-align: center;
        font-size: 1rem;
      }

      .chatbot-messages {
        flex: 1;
        overflow-y: auto;
        padding: 10px;
      }

      .chatbot-input {
        display: flex;
        gap: 5px;
        padding: 10px;
        border-top: 1px solid #ccc;
      }

      .chatbot-input input {
        flex: 1;
        padding: 8px;
        border: 1px solid #ccc;
        border-radius: 4px;
      }

      .chatbot-input button {
        padding: 8px;
        background-color: #4caf50;
        color: white;
        border: none;
        border-radius: 4px;
        cursor: pointer;
      }

      footer {
        background: #2e7d32;
        color: white;
        text-align: center;
        padding: 1.5rem 0;
        margin-top: 2rem;
      }

      footer p {
        margin: 0.5rem 0;
        font-size: 1rem;
      }

      footer a {
        color: #a5d6a7;
        text-decoration: none;
      }
    </style>
  </head>
  <body>
    <header>
      <h1>Alphabeta</h1>
      <p>Rebuilding Our Villages and Ecosystems</p>
    </header>

    <nav>
      <a href="#about">About Us</a>
      <a href="#projects">Our Projects</a>
      <a href="#join">Join Us</a>
      <a href="#contact">Contact</a>
    </nav>

    <section id="about">
      <svg class="svg-background" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100">
        <rect width="100%" height="100%" fill="#a5d6a7" />
        <circle cx="50" cy="50" r="40" fill="#4caf50" />
        <path d="M30 70 Q50 20 70 70" stroke="#2e7d32" stroke-width="2" fill="none" />
      </svg>
      <h2>About Alphabeta</h2>
      <p>
        Alphabeta is on a mission to create a greener future by empowering communities and enhancing biodiversity. Our initiatives focus on planting Tree Guilds that provide food for both wildlife and locals while improving the environment.
      </p>
    </section>

    <section id="projects">
      <svg class="svg-background" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100">
        <rect width="100%" height="100%" fill="#e8f5e9" />
        <circle cx="30" cy="50" r="20" fill="#4caf50" />
        <circle cx="70" cy="50" r="20" fill="#81c784" />
      </svg>
      <h2>Our Projects</h2>
      <p>
        - <strong>Tree Guild Planting:</strong> Strategically planting native trees to support wildlife and provide local food.<br />
        - <strong>AI for Climate:</strong> Developing AI tools to research climate change and assist in sustainable planning.<br />
        - <strong>Community Engagement:</strong> Workshops and events to raise awareness and involve locals in sustainability.
      </p>
    </section>

    <section id="join">
      <h2>Join Our Volunteer Network</h2>
      <p>
        Be part of the change! Join our network of passionate volunteers and help us make a lasting impact on our environment and community.
      </p>
      <a href="#contact">Sign Up to Volunteer</a>
    </section>

    <div id="chatbot" class="chatbot-container">
      <div class="chatbot-header">Alphabeta Assistant</div>
      <div id="chatbot-messages" class="chatbot-messages"></div>
      <div class="chatbot-input">
        <input type="text" id="user-input" placeholder="Ask me anything..." />
        <button onclick="sendMessage()">Send</button>
      </div>
    </div>

    <footer>
      <p>&copy; 2025 Alphabeta | All Rights Reserved</p>
      <p><a href="mailto:futureskool.a3@gmail.com">futureskool.a3@gmail.com</a></p>
    </footer>
  </body>
</html>
