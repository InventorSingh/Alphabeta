---
import ProductsList from "./ProductsList.astro";

const toggleChatbot = () => {
    const chatbot = document.getElementById('chatbot');
    chatbot.style.display = chatbot.style.display === 'none' ? 'flex' : 'none';
};

const sendMessage = () => {
    const userInput = document.getElementById('user-input');
    const messages = document.getElementById('chatbot-messages');

    if (userInput.value.trim() === '') return;

    const userMessage = document.createElement('div');
    userMessage.textContent = userInput.value;
    userMessage.style.textAlign = 'right';
    userMessage.style.margin = '10px';
    userMessage.style.backgroundColor = '#e0f7fa';
    userMessage.style.padding = '10px';
    userMessage.style.borderRadius = '8px';

    messages.appendChild(userMessage);

    const botMessage = document.createElement('div');
    botMessage.textContent = 'Thank you for your message! Our first AI Agent will focus on climate change research and spreading awareness on social media. Together, we can create a better world.';
    botMessage.style.margin = '10px';
    botMessage.style.backgroundColor = '#f1f1f1';
    botMessage.style.padding = '10px';
    botMessage.style.borderRadius = '8px';

    messages.appendChild(botMessage);

    userInput.value = '';
};
---

<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>FutureSkool - Empowering Tomorrow</title>
    <style>
      /* General Styles */
      body {
        font-family: Arial, sans-serif;
        margin: 0;
        padding: 0;
        color: #333;
        line-height: 1.6;
      }
      header {
        background: #4caf50;
        color: white;
        padding: 1rem 0;
        text-align: center;
      }
      header h1 {
        font-size: 2.5rem;
      }
      header p {
        margin: 0.5rem 0 1rem;
      }

      nav {
        background: #333;
        color: #fff;
        display: flex;
        justify-content: center;
        padding: 0.5rem 0;
      }
      nav a {
        color: white;
        text-decoration: none;
        margin: 0 1rem;
        font-size: 1rem;
        transition: color 0.3s;
      }
      nav a:hover {
        color: #4caf50;
      }

      section {
        padding: 2rem;
        text-align: center;
      }
      section h2 {
        font-size: 2rem;
        margin-bottom: 1rem;
        color: #4caf50;
      }
      section p {
        max-width: 800px;
        margin: 0.5rem auto;
      }

      .cta {
        background: #f4f4f4;
        padding: 2rem;
        margin: 2rem 0;
        text-align: center;
      }
      .cta a {
        display: inline-block;
        margin-top: 1rem;
        padding: 0.8rem 1.5rem;
        background: #4caf50;
        color: white;
        text-decoration: none;
        font-size: 1rem;
        border-radius: 5px;
        transition: background 0.3s;
      }
      .cta a:hover {
        background: #333;
      }

      form {
        max-width: 500px;
        margin: 0 auto;
        background: #fff;
        padding: 1.5rem;
        border-radius: 5px;
        box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
      }
      form label {
        display: block;
        margin-bottom: 0.5rem;
        font-weight: bold;
      }
      form input,
      form textarea {
        width: 100%;
        padding: 0.8rem;
        margin-bottom: 1rem;
        border: 1px solid #ccc;
        border-radius: 5px;
      }
      form button {
        width: 100%;
        padding: 0.8rem;
        background: #4caf50;
        color: white;
        border: none;
        font-size: 1rem;
        cursor: pointer;
        border-radius: 5px;
        transition: background 0.3s;
      }
      form button:hover {
        background: #333;
      }

      footer {
        background: #333;
        color: white;
        text-align: center;
        padding: 1rem 0;
        margin-top: 2rem;
      }
      footer p {
        margin: 0;
        font-size: 0.9rem;
      }
    </style>
  </head>
  <body>
    <header>
      <h1>FutureSkool</h1>
      <p>Empowering Everyone to Build a Better Tomorrow</p>
    </header>

    <nav>
      <a href="#about">About Us</a>
      <a href="#programs">Programs</a>
      <a href="#join">Join Us</a>
      <a href="#contact">Contact</a>
    </nav>

    <section id="about">
      <h2>About FutureSkool</h2>
      <p>
        At FutureSkool, our mission is to build AI Agents that help research solutions for global challenges and empower people to create a better world. Our first AI Agent will focus on climate change awareness by conducting research and spreading valuable insights on social media.
      </p>
    </section>

    <section id="programs">
      <h2>Our Projects</h2>
      <p>
        - <strong>Climate Change Awareness AI Agent</strong>: Conducts climate change research and spreads awareness through social media.<br />
        - <strong>Food Security AI Agent</strong>: Focuses on researching solutions to improve food security worldwide.
      </p>
    </section>

    <section id="join" class="cta">
      <h2>Join Our Volunteer Network</h2>
      <p>
        Help us build a better future by joining our volunteer network. As a volunteer, you can assist in shaping our AI projects and spreading impactful messages.
      </p>
      <a href="#contact">Sign Up to Volunteer</a>
    </section>

    <section id="contact">
      <h2>Contact Us</h2>
      <form id="contactForm">
        <label for="name">Your Name</label>
        <input type="text" id="name" name="name" placeholder="Enter your name" required />

        <label for="email">Your Email</label>
        <input type="email" id="email" name="email" placeholder="Enter your email" required />

        <label for="message">Your Message</label>
        <textarea id="message" name="message" rows="5" placeholder="Enter your message" required></textarea>

        <button type="submit">Send Message</button>
      </form>
    </section>

    <footer>
      <p>&copy; 2025 FutureSkool | All Rights Reserved</p>
      <p><a href="mailto:info@FutureSkool.ca" style="color: #4caf50; text-decoration: none;">info@FutureSkool.ca</a></p>
    </footer>

    <script>
      document.getElementById('contactForm').addEventListener('submit', function (e) {
        e.preventDefault();
        alert('Thank you for reaching out! We will get back to you shortly.');
        e.target.reset();
      });
    </script>
  </body>
</html>
